<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Journals by Baranex — Trade Smarter. Journal Better.</title>
<meta name="description" content="The premium forex trading journal for serious ICT & SMC traders. Track trades, master psychology, build consistency.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.0/chart.umd.min.js"></script>
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --gold:#F5A623;--gold2:#FBBF24;--dark:#06060a;--dark2:#0e0e14;
  --dark3:#16161e;--dark4:#1e1e28;--text:#F0EEE8;--muted:#7a7880;
  --green:#22c55e;--red:#ef4444;--border:rgba(245,166,35,0.14);
}
body{font-family:'Montserrat',sans-serif;background:var(--dark);color:var(--text);min-height:100vh;overflow-x:hidden}
.page{display:none;min-height:100vh}
.page.active{display:block}
/* SCROLLBAR */
::-webkit-scrollbar{width:6px}
::-webkit-scrollbar-track{background:var(--dark2)}
::-webkit-scrollbar-thumb{background:rgba(245,166,35,0.3);border-radius:3px}

/* ===== NAV ===== */
nav{position:fixed;top:0;left:0;right:0;z-index:200;padding:.9rem 2.5rem;display:flex;align-items:center;justify-content:space-between;background:rgba(6,6,10,0.93);backdrop-filter:blur(20px);border-bottom:1px solid var(--border)}
.logo-text{font-family:'Bebas Neue',sans-serif;font-size:1.5rem;letter-spacing:.06em;color:var(--gold);cursor:pointer}
.logo-text span{color:#fff}
.nav-right{display:flex;gap:.75rem;align-items:center}
.btn{padding:.5rem 1.25rem;border-radius:7px;border:none;cursor:pointer;font-family:'Montserrat',sans-serif;font-size:.78rem;font-weight:700;transition:all .2s;letter-spacing:.05em;text-transform:uppercase}
.btn-outline{background:transparent;border:1px solid rgba(245,166,35,0.25);color:var(--text)}
.btn-outline:hover{border-color:var(--gold);color:var(--gold)}
.btn-gold{background:var(--gold);color:#000}
.btn-gold:hover{background:var(--gold2);transform:translateY(-1px);box-shadow:0 8px 24px rgba(245,166,35,0.2)}
.btn-full{width:100%;padding:.85rem;font-size:.85rem}

/* ===== HERO ===== */
.hero{min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:6rem 1.5rem 3rem;position:relative;overflow:hidden}
.hero-bg{position:absolute;inset:0;background:var(--dark)}
.hero-glow{position:absolute;top:40%;left:50%;transform:translate(-50%,-50%);width:700px;height:500px;background:radial-gradient(ellipse,rgba(245,166,35,0.08) 0%,transparent 65%);pointer-events:none}
.hero-grid-lines{position:absolute;inset:0;background-image:linear-gradient(rgba(245,166,35,0.03) 1px,transparent 1px),linear-gradient(90deg,rgba(245,166,35,0.03) 1px,transparent 1px);background-size:70px 70px;pointer-events:none}
.grid-boxes{position:absolute;inset:0;pointer-events:none;overflow:hidden}
.gbox{position:absolute;border:1px solid rgba(245,166,35,0.07);border-radius:8px;animation:gboxFloat linear infinite}
@keyframes gboxFloat{0%{opacity:0;transform:translateY(0) rotate(var(--r))}15%{opacity:1}85%{opacity:.4}100%{opacity:0;transform:translateY(-110vh) rotate(calc(var(--r) + 20deg))}}

.hero-content{position:relative;z-index:2}
.badge-pill{display:inline-flex;align-items:center;gap:.5rem;padding:.35rem 1rem;border-radius:20px;border:1px solid rgba(245,166,35,0.2);font-size:.65rem;color:var(--gold);margin-bottom:1.75rem;background:rgba(245,166,35,0.05);letter-spacing:.1em;text-transform:uppercase;font-weight:700}
.main-title{font-family:'Bebas Neue',sans-serif;font-size:clamp(3.5rem,11vw,8rem);letter-spacing:.04em;line-height:.92;margin-bottom:.2em}
.main-title .gold{color:var(--gold)}
.sub-line{font-size:clamp(.85rem,2vw,1.1rem);font-weight:300;color:rgba(255,255,255,0.6);letter-spacing:.15em;text-transform:uppercase;margin-bottom:2rem}

/* JOURNAL ANIMATION */
.journal-box{background:rgba(14,14,20,0.97);border:1px solid rgba(245,166,35,0.2);border-radius:14px;max-width:520px;margin:0 auto 2.5rem;overflow:hidden;box-shadow:0 0 60px rgba(245,166,35,0.05)}
.jbar{background:var(--dark3);padding:.6rem 1rem;display:flex;align-items:center;gap:.5rem;border-bottom:1px solid var(--border)}
.jdot{width:9px;height:9px;border-radius:50%;background:rgba(245,166,35,0.25)}
.jtitle{font-size:.65rem;color:var(--muted);margin-left:.3rem;letter-spacing:.08em;text-transform:uppercase;font-weight:700}
.jbody{padding:1.1rem 1.35rem;text-align:left;min-height:140px}
.jline{display:flex;align-items:flex-start;gap:.5rem;margin-bottom:.6rem;font-size:.8rem;line-height:1.55;opacity:0;transform:translateX(-10px);transition:all .45s ease}
.jline.show{opacity:1;transform:translateX(0)}
.jtag{font-size:.62rem;font-weight:700;letter-spacing:.06em;padding:.15rem .5rem;border-radius:4px;white-space:nowrap;margin-top:.05rem}
.jtag.inst{background:rgba(245,166,35,0.15);color:var(--gold)}
.jtag.res{background:rgba(34,197,94,0.12);color:var(--green)}
.jtag.loss-t{background:rgba(239,68,68,0.12);color:var(--red)}
.jtag.note{background:rgba(255,255,255,0.06);color:var(--muted)}
.jtext{color:var(--text);flex:1}
.jtext .hi{color:var(--gold);font-weight:600}
.jtext .gr{color:var(--green);font-weight:600}
.cursor-blink{display:inline-block;width:2px;height:.85em;background:var(--gold);vertical-align:middle;animation:cblink .75s step-end infinite;margin-left:2px}
@keyframes cblink{0%,100%{opacity:1}50%{opacity:0}}

.hero-desc{font-size:.88rem;color:var(--muted);max-width:440px;margin:0 auto 2rem;line-height:1.8;font-weight:300}
.hero-btns{display:flex;gap:1rem;justify-content:center;flex-wrap:wrap}
.hero-btns .btn{padding:.8rem 2.2rem;font-size:.78rem;letter-spacing:.08em}

/* ===== FEATURES ===== */
.section{padding:5rem 2rem;max-width:1100px;margin:0 auto}
.sec-title{font-family:'Bebas Neue',sans-serif;font-size:clamp(2rem,5vw,3.5rem);text-align:center;margin-bottom:.3rem;letter-spacing:.06em;color:var(--gold)}
.sec-sub{text-align:center;color:var(--muted);margin-bottom:3rem;font-size:.85rem;letter-spacing:.03em}
.feat-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:1.25rem}
.feat-card{background:var(--dark2);border:1px solid var(--border);border-radius:14px;padding:1.5rem;transition:all .3s}
.feat-card:hover{border-color:rgba(245,166,35,0.35);transform:translateY(-4px)}
.feat-icon{font-size:1.5rem;margin-bottom:.8rem}
.feat-card h3{font-size:.9rem;font-weight:700;margin-bottom:.4rem;letter-spacing:.02em}
.feat-card p{font-size:.8rem;color:var(--muted);line-height:1.65;font-weight:400}

/* ===== PRICING ===== */
.pricing-card{background:var(--dark2);border:2px solid rgba(245,166,35,0.4);border-radius:20px;padding:2.5rem;max-width:420px;margin:0 auto;text-align:center;position:relative}
.p-badge{display:inline-block;background:var(--gold);color:#000;font-size:.62rem;font-weight:800;padding:.3rem 1rem;border-radius:20px;letter-spacing:.08em;text-transform:uppercase;margin-bottom:1.5rem}
.p-amount{font-family:'Bebas Neue',sans-serif;font-size:5rem;color:var(--gold);line-height:1;letter-spacing:.02em}
.p-amount sup{font-family:'Montserrat',sans-serif;font-size:2rem;font-weight:700;vertical-align:top;margin-top:.8rem;display:inline-block}
.p-note{color:var(--muted);font-size:.75rem;margin-bottom:1.75rem;letter-spacing:.06em;text-transform:uppercase}
.p-list{list-style:none;text-align:left;margin-bottom:2rem}
.p-list li{padding:.5rem 0;font-size:.875rem;display:flex;align-items:center;gap:.6rem;border-bottom:1px solid rgba(255,255,255,0.04)}
.p-list li::before{content:"✓";color:var(--gold);font-weight:900}

/* ===== FOOTER ===== */
footer{border-top:1px solid var(--border);padding:2rem;text-align:center;color:var(--muted);font-size:.78rem;line-height:2}
footer .fn{font-family:'Bebas Neue',sans-serif;color:var(--gold);font-size:1.1rem;letter-spacing:.06em}

/* ===== AUTH ===== */
.auth-page{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:2rem;padding-top:6rem}
.auth-box{background:var(--dark2);border:1px solid var(--border);border-radius:18px;padding:2.5rem;width:100%;max-width:400px}
.auth-box h2{font-family:'Bebas Neue',sans-serif;font-size:2rem;letter-spacing:.05em;margin-bottom:.25rem}
.auth-box p{color:var(--muted);font-size:.82rem;margin-bottom:2rem}
.auth-tabs{display:flex;background:var(--dark3);border-radius:8px;padding:3px;margin-bottom:1.5rem}
.atab{flex:1;padding:.5rem;text-align:center;border-radius:6px;cursor:pointer;font-size:.75rem;color:var(--muted);transition:all .2s;font-weight:700;letter-spacing:.05em;text-transform:uppercase}
.atab.active{background:var(--gold);color:#000}
.fg{margin-bottom:1rem}
.fg label{display:block;font-size:.68rem;color:var(--muted);margin-bottom:.4rem;letter-spacing:.07em;text-transform:uppercase;font-weight:700}
.fg input{width:100%;padding:.75rem 1rem;background:var(--dark3);border:1px solid var(--border);border-radius:8px;color:var(--text);font-family:'Montserrat',sans-serif;font-size:.875rem;outline:none;transition:border .2s}
.fg input:focus{border-color:var(--gold)}
.auth-msg{text-align:center;font-size:.8rem;padding:.6rem;border-radius:6px;margin-bottom:.75rem;display:none}
.auth-msg.err{background:rgba(239,68,68,0.1);color:var(--red);display:block}
.auth-msg.ok{background:rgba(34,197,94,0.1);color:var(--green);display:block}

/* ===== APP ===== */
.app-layout{display:flex;min-height:100vh}
.sidebar{width:230px;background:var(--dark2);border-right:1px solid var(--border);padding:1.5rem 1rem;position:fixed;top:0;left:0;bottom:0;z-index:50;display:flex;flex-direction:column;gap:.2rem;overflow-y:auto}
.sb-logo{font-family:'Bebas Neue',sans-serif;font-size:1.1rem;color:var(--gold);letter-spacing:.05em;padding:.75rem 1rem 1.5rem;line-height:1.3}
.sb-logo span{display:block;font-family:'Montserrat',sans-serif;font-size:.6rem;color:var(--muted);font-weight:500;letter-spacing:.04em;margin-top:.2rem}
.ni{display:flex;align-items:center;gap:.75rem;padding:.65rem 1rem;border-radius:8px;cursor:pointer;font-size:.8rem;color:var(--muted);transition:all .2s;font-weight:600;letter-spacing:.02em}
.ni:hover,.ni.active{background:rgba(245,166,35,0.1);color:var(--gold)}
.ni .ic{font-size:.9rem;width:18px;text-align:center}
.mc{margin-left:230px;flex:1;padding:6rem 2rem 2rem;min-height:100vh}
.ph{margin-bottom:2rem}
.ph h2{font-family:'Bebas Neue',sans-serif;font-size:1.9rem;letter-spacing:.04em}
.ph p{color:var(--muted);font-size:.8rem;margin-top:.2rem}

/* STATS */
.sg{display:grid;grid-template-columns:repeat(auto-fit,minmax(145px,1fr));gap:1rem;margin-bottom:2rem}
.sc{background:var(--dark2);border:1px solid var(--border);border-radius:12px;padding:1.25rem}
.sl{font-size:.63rem;color:var(--muted);margin-bottom:.5rem;text-transform:uppercase;letter-spacing:.08em;font-weight:700}
.sv{font-family:'Bebas Neue',sans-serif;font-size:2.2rem;letter-spacing:.03em}
.sv.gold{color:var(--gold)}.sv.green{color:var(--green)}.sv.red{color:var(--red)}
.sch{font-size:.7rem;color:var(--muted);margin-top:.2rem}

/* CHARTS */
.cg{display:grid;grid-template-columns:2fr 1fr;gap:1rem;margin-bottom:2rem}
.cb{background:var(--dark2);border:1px solid var(--border);border-radius:12px;padding:1.5rem}
.cb h3{font-size:.63rem;color:var(--muted);margin-bottom:1.25rem;text-transform:uppercase;letter-spacing:.1em;font-weight:700}

/* TABLE */
.tw{background:var(--dark2);border:1px solid var(--border);border-radius:12px;overflow:hidden;margin-bottom:1.5rem}
.th2{padding:1rem 1.5rem;display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid var(--border)}
.th2 h3{font-family:'Bebas Neue',sans-serif;font-size:1.05rem;letter-spacing:.05em;color:var(--gold)}
table{width:100%;border-collapse:collapse}
th{padding:.65rem 1rem;font-size:.62rem;color:var(--muted);text-align:left;border-bottom:1px solid var(--border);font-weight:700;text-transform:uppercase;letter-spacing:.06em}
td{padding:.75rem 1rem;font-size:.78rem;border-bottom:1px solid rgba(255,255,255,0.03)}
tr:last-child td{border:none}
tr:hover td{background:rgba(245,166,35,0.02)}
.bw{background:rgba(34,197,94,0.12);color:var(--green);padding:.2rem .6rem;border-radius:4px;font-size:.65rem;font-weight:700;letter-spacing:.04em}
.bl{background:rgba(239,68,68,0.12);color:var(--red);padding:.2rem .6rem;border-radius:4px;font-size:.65rem;font-weight:700;letter-spacing:.04em}
.bb{background:rgba(245,166,35,0.12);color:var(--gold);padding:.2rem .6rem;border-radius:4px;font-size:.65rem;font-weight:700}
.db{color:var(--green);font-weight:700}.ds{color:var(--red);font-weight:700}

/* FORM */
.fgrid{display:grid;grid-template-columns:repeat(auto-fit,minmax(195px,1fr));gap:1rem}
.frow{background:var(--dark2);border:1px solid var(--border);border-radius:12px;padding:1.5rem;margin-bottom:1rem}
.frow h3{font-family:'Bebas Neue',sans-serif;font-size:.9rem;letter-spacing:.08em;color:var(--gold);margin-bottom:1rem}
.finput{width:100%;padding:.7rem 1rem;background:var(--dark3);border:1px solid var(--border);border-radius:8px;color:var(--text);font-family:'Montserrat',sans-serif;font-size:.825rem;outline:none;transition:border .2s}
.finput:focus{border-color:var(--gold)}
textarea.finput{resize:vertical;min-height:80px}
.stars{display:flex;gap:.3rem;font-size:1.5rem;cursor:pointer}
.star{color:var(--dark4);transition:color .15s}.star.on{color:var(--gold)}
.mtags{display:flex;flex-wrap:wrap;gap:.5rem}
.mtag{padding:.3rem .75rem;border-radius:20px;border:1px solid var(--border);font-size:.7rem;cursor:pointer;color:var(--muted);transition:all .2s;font-weight:600}
.mtag.on{border-color:var(--gold);color:var(--gold);background:rgba(245,166,35,0.1)}

/* CALENDAR */
.ch{text-align:center;font-size:.62rem;color:var(--muted);padding:.5rem 0;font-weight:700;letter-spacing:.06em;text-transform:uppercase}
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px}
.cday{aspect-ratio:1;border-radius:8px;display:flex;align-items:center;justify-content:center;flex-direction:column;font-size:.65rem;border:1px solid var(--border);background:var(--dark2);cursor:pointer;transition:.2s;font-weight:600}
.cday:hover{border-color:var(--gold)}
.cday.win{background:rgba(34,197,94,0.08);border-color:rgba(34,197,94,0.25)}
.cday.loss{background:rgba(239,68,68,0.08);border-color:rgba(239,68,68,0.25)}
.cday .dp{font-size:.58rem;color:var(--green)}
.cday.loss .dp{color:var(--red)}

/* ANALYTICS */
.ag{display:grid;grid-template-columns:repeat(auto-fit,minmax(290px,1fr));gap:1rem}

/* LOCK */
.lock-banner{background:var(--dark2);border:1px solid rgba(245,166,35,0.4);border-radius:14px;padding:2rem;text-align:center;margin-bottom:1.5rem}
.lock-banner h3{font-family:'Bebas Neue',sans-serif;font-size:1.4rem;letter-spacing:.05em;margin-bottom:.5rem}
.lock-banner p{color:var(--muted);font-size:.85rem;margin-bottom:1.25rem}

/* BOTTOM NAV */
.bnav{display:none;position:fixed;bottom:0;left:0;right:0;background:var(--dark2);border-top:1px solid var(--border);padding:.5rem;z-index:100;justify-content:space-around}
.bni{display:flex;flex-direction:column;align-items:center;gap:.2rem;padding:.4rem .8rem;border-radius:8px;cursor:pointer;font-size:.52rem;color:var(--muted);font-weight:700;letter-spacing:.04em;text-transform:uppercase}
.bni.active{color:var(--gold)}
.bni .ic{font-size:1rem}

/* MOBILE */
@media(max-width:768px){
  .sidebar{display:none}
  .mc{margin-left:0;padding:5rem 1rem 5rem}
  .bnav{display:flex}
  .cg{grid-template-columns:1fr}
  nav{padding:.75rem 1rem}
  .hero{padding:5rem 1rem 3rem}
  .journal-box{max-width:100%}
}

/* FADE IN */
@keyframes fadeUp{from{opacity:0;transform:translateY(22px)}to{opacity:1;transform:translateY(0)}}
.au{opacity:0;animation:fadeUp .65s ease forwards}
.d1{animation-delay:.05s}.d2{animation-delay:.18s}.d3{animation-delay:.32s}.d4{animation-delay:.46s}.d5{animation-delay:.58s}
</style>
</head>
<body>

<!-- ============ LANDING PAGE ============ -->
<div id="page-landing" class="page active">
  <nav>
    <div class="logo-text" onclick="showPage('page-landing')">JOURNALS <span>BY BARANEX</span></div>
    <div class="nav-right">
      <button class="btn btn-outline" onclick="showPage('page-auth')">LOGIN</button>
      <button class="btn btn-gold" onclick="showPage('page-auth')">GET STARTED</button>
    </div>
  </nav>

  <div class="hero">
    <div class="hero-bg"></div>
    <div class="hero-grid-lines"></div>
    <div class="grid-boxes" id="gboxes"></div>
    <div class="hero-glow"></div>

    <div class="hero-content">
      <div class="badge-pill au d1">★ BUILT FOR SERIOUS FOREX TRADERS</div>

      <div class="au d2">
        <div class="main-title"><span class="gold">JOURNALS</span> BY BARANEX</div>
        <div class="sub-line">Trade Smarter. Journal Better.</div>
      </div>

      <!-- JOURNAL TYPEWRITER -->
      <div class="journal-box au d3">
        <div class="jbar">
          <div class="jdot"></div><div class="jdot"></div><div class="jdot"></div>
          <span class="jtitle">MY TRADE JOURNAL — LIVE ENTRY</span>
        </div>
        <div class="jbody">
          <div class="jline" id="jl0"><span class="jtag inst">US30</span><span class="jtext">BUY @ <span class="hi">42,180</span> · SL 42,100 · TP 42,340 · R:R <span class="hi">1:2</span></span></div>
          <div class="jline" id="jl1"><span class="jtag note">SESSION</span><span class="jtext">New York · Strategy: <span class="hi">FVG + CHoCH</span> · Lot 0.10</span></div>
          <div class="jline" id="jl2"><span class="jtag note">MINDSET</span><span class="jtext" id="jt2"></span><span class="cursor-blink" id="jc2" style="display:none"></span></div>
          <div class="jline" id="jl3"><span class="jtag res">WIN ✓</span><span class="jtext">+<span class="gr">2R</span> · Emotion ⭐⭐⭐⭐⭐ · <span class="hi">Perfect Execution</span></span></div>
          <div class="jline" id="jl4"><span class="jtag note">LESSON</span><span class="jtext" id="jt4"></span><span class="cursor-blink" id="jc4" style="display:none"></span></div>
        </div>
      </div>

      <p class="hero-desc au d4">The premium forex trading journal for ICT & SMC traders. Track trades, master psychology, and build unbreakable consistency.</p>

      <div class="hero-btns au d5">
        <button class="btn btn-gold" onclick="showPage('page-auth')" style="padding:.8rem 2.2rem;letter-spacing:.07em">START JOURNALING — FREE</button>
        <button class="btn btn-outline" onclick="document.getElementById('pricing-sec').scrollIntoView({behavior:'smooth'})" style="padding:.8rem 2rem;letter-spacing:.06em">VIEW PRICING</button>
      </div>
    </div>
  </div>

  <!-- FEATURES -->
  <div class="section">
    <div class="sec-title">EVERYTHING YOU NEED</div>
    <p class="sec-sub">Not just a spreadsheet — a complete trading intelligence system</p>
    <div class="feat-grid">
      <div class="feat-card"><div class="feat-icon">📊</div><h3>Equity Curve & Analytics</h3><p>Visual equity curve, win rate, profit factor, and session-wise performance — all automated from your trades.</p></div>
      <div class="feat-card"><div class="feat-icon">🧠</div><h3>Psychology Tracking</h3><p>Log pre-trade mindset, emotional rating, and mistake tags. Identify and eliminate FOMO and revenge trading.</p></div>
      <div class="feat-card"><div class="feat-icon">⚡</div><h3>MT5 CSV Import</h3><p>Upload your MT5 trade history and all trades auto-fill instantly. Zero manual entry needed.</p></div>
      <div class="feat-card"><div class="feat-icon">📅</div><h3>P&L Calendar</h3><p>Color-coded daily P&L calendar. See your best and worst days instantly and spot patterns.</p></div>
      <div class="feat-card"><div class="feat-icon">📱</div><h3>Mobile First Design</h3><p>Journal trades from anywhere. Fully responsive — works perfectly on phone, tablet, and desktop.</p></div>
      <div class="feat-card"><div class="feat-icon">🔐</div><h3>Lifetime Access</h3><p>Pay once. Use forever. No monthly fees, no subscriptions. Your data, always yours.</p></div>
    </div>
  </div>

  <!-- PRICING -->
  <div class="section" id="pricing-sec">
    <div class="sec-title">SIMPLE PRICING</div>
    <p class="sec-sub">One time payment. Lifetime access. No BS.</p>
    <div class="pricing-card">
      <div class="p-badge">LIFETIME DEAL</div>
      <div class="p-amount"><sup>₹</sup>999</div>
      <div class="p-note">One-time · Use Forever</div>
      <ul class="p-list">
        <li>Unlimited trade logs</li>
        <li>Equity curve & full analytics</li>
        <li>Psychology & emotion tracking</li>
        <li>MT5 CSV import</li>
        <li>Trade calendar view</li>
        <li>Session & instrument analytics</li>
        <li>Screenshot uploads per trade</li>
        <li>All future updates free</li>
      </ul>
      <button class="btn btn-gold btn-full" onclick="handlePayment()" style="letter-spacing:.07em">BUY LIFETIME ACCESS — ₹999</button>
    </div>
  </div>

  <footer>
    <div class="fn">JOURNALS BY BARANEX</div>
    <div>Built by <strong style="color:var(--gold)">Aditya Barnwal</strong> &nbsp;·&nbsp; For traders, by a trader</div>
    <div style="margin-top:.3rem">© 2025 Baranex · All rights reserved</div>
  </footer>
</div>

<!-- ============ AUTH PAGE ============ -->
<div id="page-auth" class="page">
  <nav>
    <div class="logo-text" onclick="showPage('page-landing')">JOURNALS <span>BY BARANEX</span></div>
  </nav>
  <div class="auth-page">
    <div class="auth-box">
      <h2>WELCOME BACK</h2>
      <p>Sign in to your trading journal</p>
      <div class="auth-tabs">
        <div class="atab active" onclick="switchTab(this,'login')">LOGIN</div>
        <div class="atab" onclick="switchTab(this,'signup')">SIGN UP</div>
      </div>

      <div id="auth-msg" class="auth-msg"></div>

      <div id="login-form">
        <div class="fg"><label>Email</label><input type="email" id="login-email" placeholder="you@email.com"></div>
        <div class="fg"><label>Password</label><input type="password" id="login-pass" placeholder="••••••••"></div>
        <button class="btn btn-gold btn-full" style="margin-top:.5rem;letter-spacing:.06em" onclick="handleLogin()">LOGIN TO JOURNAL</button>
      </div>

      <div id="signup-form" style="display:none">
        <div class="fg"><label>Full Name</label><input type="text" id="signup-name" placeholder="Your name"></div>
        <div class="fg"><label>Email</label><input type="email" id="signup-email" placeholder="you@email.com"></div>
        <div class="fg"><label>Password</label><input type="password" id="signup-pass" placeholder="Min 6 characters"></div>
        <button class="btn btn-gold btn-full" style="margin-top:.5rem;letter-spacing:.06em" onclick="handleSignup()">CREATE ACCOUNT</button>
      </div>
    </div>
  </div>
</div>

<!-- ============ LOCKED PAGE ============ -->
<div id="page-locked" class="page">
  <nav>
    <div class="logo-text">JOURNALS <span>BY BARANEX</span></div>
    <div class="nav-right">
      <button class="btn btn-gold" onclick="handlePayment()">UNLOCK ₹999</button>
      <button class="btn btn-outline" onclick="handleLogout()">LOGOUT</button>
    </div>
  </nav>
  <div style="padding:7rem 2rem 2rem;max-width:650px;margin:0 auto">
    <div class="lock-banner">
      <div style="font-size:2.5rem;margin-bottom:.5rem">🔒</div>
      <h3>UNLOCK LIFETIME ACCESS</h3>
      <p>You're one step away from complete trade tracking, equity curves, analytics and psychology insights.</p>
      <button class="btn btn-gold" style="padding:.8rem 2rem;font-size:.82rem;letter-spacing:.06em" onclick="handlePayment()">GET LIFETIME ACCESS — ₹999</button>
    </div>
    <div style="filter:blur(5px);pointer-events:none">
      <div class="sg">
        <div class="sc"><div class="sl">Total Trades</div><div class="sv gold">47</div></div>
        <div class="sc"><div class="sl">Win Rate</div><div class="sv green">68%</div></div>
        <div class="sc"><div class="sl">Profit Factor</div><div class="sv gold">2.1</div></div>
        <div class="sc"><div class="sl">Total R</div><div class="sv green">+18R</div></div>
      </div>
    </div>
  </div>
</div>

<!-- ============ FULL DASHBOARD ============ -->
<div id="page-dashboard" class="page">
  <nav id="appnav" style="left:230px">
    <div class="logo-text">JOURNALS <span>BY BARANEX</span></div>
    <div class="nav-right">
      <span style="font-size:.7rem;color:var(--gold);font-weight:700;letter-spacing:.05em">★ LIFETIME MEMBER</span>
      <button class="btn btn-outline" onclick="handleLogout()" style="font-size:.7rem;letter-spacing:.04em">LOGOUT</button>
    </div>
  </nav>

  <div class="app-layout">
    <div class="sidebar">
      <div class="sb-logo">JOURNALS BY BARANEX<span>by Aditya Barnwal</span></div>
      <div class="ni active" onclick="swApp('dashboard',this)"><span class="ic">📊</span>Dashboard</div>
      <div class="ni" onclick="swApp('tradelog',this)"><span class="ic">📝</span>Trade Log</div>
      <div class="ni" onclick="swApp('analytics',this)"><span class="ic">📈</span>Analytics</div>
      <div class="ni" onclick="swApp('calendar',this)"><span class="ic">📅</span>Calendar</div>
      <div class="ni" onclick="swApp('addtrade',this)"><span class="ic">➕</span>Add Trade</div>
      <div style="flex:1"></div>
      <div class="ni" onclick="handleLogout()"><span class="ic">🚪</span>Logout</div>
    </div>

    <!-- DASHBOARD VIEW -->
    <div class="mc" id="v-dashboard">
      <div class="ph"><h2 id="greet-msg">GOOD EVENING, TRADER 👋</h2><p>Your trading performance overview</p></div>
      <div class="sg">
        <div class="sc"><div class="sl">Total Trades</div><div class="sv gold" id="stat-trades">0</div><div class="sch">This month</div></div>
        <div class="sc"><div class="sl">Win Rate</div><div class="sv green" id="stat-wr">0%</div><div class="sch">Based on all trades</div></div>
        <div class="sc"><div class="sl">Profit Factor</div><div class="sv gold" id="stat-pf">0</div><div class="sch">Target: 2.0+</div></div>
        <div class="sc"><div class="sl">Total R Gained</div><div class="sv green" id="stat-r">+0R</div><div class="sch">Cumulative</div></div>
      </div>
      <div class="cg">
        <div class="cb"><h3>EQUITY CURVE</h3><canvas id="eq" height="170"></canvas></div>
        <div class="cb"><h3>WIN / LOSS</h3><canvas id="wl" height="170"></canvas></div>
      </div>
      <div class="tw">
        <div class="th2"><h3>RECENT TRADES</h3><button class="btn btn-outline" style="font-size:.68rem;padding:.3rem .8rem;letter-spacing:.04em" onclick="swApp('tradelog',null)">VIEW ALL</button></div>
        <div style="overflow-x:auto"><table><thead><tr><th>Date</th><th>Pair</th><th>Dir</th><th>Session</th><th>Strategy</th><th>R:R</th><th>Result</th></tr></thead>
        <tbody id="recent-trades-body">
          <tr><td colspan="7" style="text-align:center;color:var(--muted);padding:2rem">No trades yet. Add your first trade!</td></tr>
        </tbody></table></div>
      </div>
    </div>

    <!-- TRADE LOG VIEW -->
    <div class="mc" id="v-tradelog" style="display:none">
      <div class="ph"><h2>TRADE LOG</h2><p>All your trades in one place</p></div>
      <div style="display:flex;gap:.75rem;margin-bottom:1rem;flex-wrap:wrap;align-items:center">
        <select class="finput" style="width:auto;padding:.4rem .8rem;font-size:.75rem"><option>All Instruments</option><option>US30</option><option>XAUUSD</option><option>EURUSD</option></select>
        <select class="finput" style="width:auto;padding:.4rem .8rem;font-size:.75rem"><option>All Sessions</option><option>London</option><option>New York</option><option>Asian</option></select>
        <select class="finput" style="width:auto;padding:.4rem .8rem;font-size:.75rem"><option>All Results</option><option>Win</option><option>Loss</option><option>Breakeven</option></select>
        <button class="btn btn-gold" style="padding:.4rem 1rem;font-size:.72rem;letter-spacing:.05em" onclick="swApp('addtrade',null)">+ ADD TRADE</button>
      </div>
      <div class="tw"><div style="overflow-x:auto"><table>
        <thead><tr><th>Date</th><th>Pair</th><th>Dir</th><th>Session</th><th>Entry</th><th>SL</th><th>TP</th><th>R:R</th><th>Strategy</th><th>Emotion</th><th>Result</th></tr></thead>
        <tbody id="all-trades-body"><tr><td colspan="11" style="text-align:center;color:var(--muted);padding:2rem">No trades yet.</td></tr></tbody>
      </table></div></div>
    </div>

    <!-- ADD TRADE VIEW -->
    <div class="mc" id="v-addtrade" style="display:none">
      <div class="ph"><h2>ADD NEW TRADE</h2><p>Log every detail for maximum insight</p></div>
      <div class="frow">
        <h3>TRADE DETAILS</h3>
        <div class="fgrid">
          <div class="fg"><label>Date & Time</label><input type="datetime-local" class="finput" id="f-date"></div>
          <div class="fg"><label>Instrument</label><select class="finput" id="f-inst"><option>US30</option><option>XAUUSD</option><option>EURUSD</option><option>GBPUSD</option><option>NAS100</option></select></div>
          <div class="fg"><label>Session</label><select class="finput" id="f-sess"><option>New York</option><option>London</option><option>Asian</option><option>Overlap</option></select></div>
          <div class="fg"><label>Direction</label><select class="finput" id="f-dir"><option>BUY</option><option>SELL</option></select></div>
          <div class="fg"><label>Entry Price</label><input type="number" class="finput" id="f-entry" placeholder="42180" oninput="calcRR()"></div>
          <div class="fg"><label>Stop Loss</label><input type="number" class="finput" id="f-sl" placeholder="42100" oninput="calcRR()"></div>
          <div class="fg"><label>Take Profit</label><input type="number" class="finput" id="f-tp" placeholder="42340" oninput="calcRR()"></div>
          <div class="fg"><label>R:R (auto)</label><input type="text" class="finput" id="f-rr" value="—" readonly style="border-color:var(--gold);color:var(--gold);background:rgba(245,166,35,0.05)"></div>
          <div class="fg"><label>Lot Size</label><input type="number" class="finput" id="f-lot" placeholder="0.10" step="0.01"></div>
          <div class="fg"><label>Result</label><select class="finput" id="f-res"><option>Win</option><option>Loss</option><option>Breakeven</option></select></div>
          <div class="fg"><label>Strategy Tag</label><select class="finput" id="f-strat"><option>FVG</option><option>Order Block</option><option>CHoCH</option><option>Liquidity Sweep</option><option>BOS</option><option>MSS</option></select></div>
          <div class="fg"><label>Pips +/-</label><input type="number" class="finput" id="f-pips" placeholder="+80"></div>
        </div>
      </div>
      <div class="frow">
        <h3>PSYCHOLOGY</h3>
        <div class="fg"><label>Pre-Trade Mindset — What was your mental state before entry?</label><textarea class="finput" id="f-pre" placeholder="Was I confident? Any FOMO? Did I follow my rules before entering? Was I patient or rushing?"></textarea></div>
        <div class="fg"><label>Post-Trade Notes & Lesson</label><textarea class="finput" id="f-post" placeholder="Did price follow my plan? Did I manage the trade properly? What is my lesson from this trade?"></textarea></div>
        <div class="fg"><label>Emotional Discipline (1-5 Stars)</label>
          <div class="stars" id="stars">
            <span class="star on" onclick="rateStar(1)">★</span><span class="star on" onclick="rateStar(2)">★</span>
            <span class="star on" onclick="rateStar(3)">★</span><span class="star" onclick="rateStar(4)">★</span><span class="star" onclick="rateStar(5)">★</span>
          </div>
        </div>
        <div class="fg"><label>Mistake Tag (select if applicable)</label>
          <div class="mtags">
            <span class="mtag" onclick="this.classList.toggle('on')">FOMO</span>
            <span class="mtag" onclick="this.classList.toggle('on')">Revenge Trade</span>
            <span class="mtag" onclick="this.classList.toggle('on')">Moved SL</span>
            <span class="mtag" onclick="this.classList.toggle('on')">Overleverage</span>
            <span class="mtag on" onclick="this.classList.toggle('on')">Perfect Execution</span>
            <span class="mtag" onclick="this.classList.toggle('on')">Early Entry</span>
            <span class="mtag" onclick="this.classList.toggle('on')">Late Entry</span>
          </div>
        </div>
      </div>
      <div class="frow">
        <h3>SCREENSHOT</h3>
        <div id="ss-drop" style="border:2px dashed rgba(245,166,35,0.2);border-radius:10px;padding:2rem;text-align:center;cursor:pointer;color:var(--muted);font-size:.85rem;transition:.2s" onclick="document.getElementById('ss-input').click()" ondragover="event.preventDefault();this.style.borderColor='var(--gold)'" ondrop="handleDrop(event)">
          📸 Click or drag to upload trade screenshot<br><span style="font-size:.72rem;margin-top:.4rem;display:block">PNG, JPG up to 5MB</span>
          <input type="file" id="ss-input" accept="image/*" style="display:none" onchange="handleFile(this.files[0])">
        </div>
        <div id="ss-preview" style="display:none;margin-top:.75rem"><img id="ss-img" style="max-width:100%;border-radius:8px;border:1px solid var(--border)"></div>
      </div>
      <div id="save-msg" class="auth-msg" style="margin-bottom:.75rem"></div>
      <button class="btn btn-gold" id="save-btn" style="width:100%;padding:1rem;font-size:.88rem;margin-top:.5rem;letter-spacing:.07em" onclick="saveTrade()">SAVE TRADE ✓</button>
    </div>

    <!-- ANALYTICS VIEW -->
    <div class="mc" id="v-analytics" style="display:none">
      <div class="ph"><h2>ANALYTICS</h2><p>Deep dive into your trading performance</p></div>
      <div class="sg" style="margin-bottom:1.5rem">
        <div class="sc"><div class="sl">Best Session</div><div class="sv gold" id="a-sess" style="font-size:1.3rem">—</div></div>
        <div class="sc"><div class="sl">Best Instrument</div><div class="sv gold" id="a-inst" style="font-size:1.3rem">—</div></div>
        <div class="sc"><div class="sl">Win Streak</div><div class="sv green" id="a-streak">0</div></div>
        <div class="sc"><div class="sl">Avg Emotion</div><div class="sv gold" id="a-emo">0★</div></div>
      </div>
      <div class="ag">
        <div class="cb"><h3>P&L BY INSTRUMENT</h3><canvas id="ic" height="200"></canvas></div>
        <div class="cb"><h3>P&L BY SESSION</h3><canvas id="scc" height="200"></canvas></div>
        <div class="cb"><h3>MISTAKE ANALYSIS</h3><canvas id="mcc" height="200"></canvas></div>
        <div class="cb"><h3>STRATEGY WIN RATE %</h3><canvas id="stcc" height="200"></canvas></div>
      </div>
    </div>

    <!-- CALENDAR VIEW -->
    <div class="mc" id="v-calendar" style="display:none">
      <div class="ph"><h2>TRADE CALENDAR</h2><p>Daily P&L Overview</p></div>
      <div class="cb">
        <div style="display:grid;grid-template-columns:repeat(7,1fr);margin-bottom:.5rem">
          <div class="ch">MON</div><div class="ch">TUE</div><div class="ch">WED</div>
          <div class="ch">THU</div><div class="ch">FRI</div><div class="ch">SAT</div><div class="ch">SUN</div>
        </div>
        <div class="cal-grid" id="calg"></div>
      </div>
      <div style="display:flex;gap:1.5rem;font-size:.7rem;color:var(--muted);margin-top:1rem;font-weight:700;letter-spacing:.04em">
        <span style="display:flex;align-items:center;gap:.4rem"><span style="width:12px;height:12px;border-radius:3px;background:rgba(34,197,94,0.15);display:inline-block;border:1px solid rgba(34,197,94,0.3)"></span>PROFITABLE</span>
        <span style="display:flex;align-items:center;gap:.4rem"><span style="width:12px;height:12px;border-radius:3px;background:rgba(239,68,68,0.15);display:inline-block;border:1px solid rgba(239,68,68,0.3)"></span>LOSS DAY</span>
        <span style="display:flex;align-items:center;gap:.4rem"><span style="width:12px;height:12px;border-radius:3px;background:var(--dark2);display:inline-block;border:1px solid var(--border)"></span>NO TRADES</span>
      </div>
    </div>
  </div>

  <div class="bnav">
    <div class="bni active" onclick="swApp('dashboard',this)"><span class="ic">📊</span>HOME</div>
    <div class="bni" onclick="swApp('tradelog',this)"><span class="ic">📝</span>TRADES</div>
    <div class="bni" onclick="swApp('addtrade',this)"><span class="ic">➕</span>ADD</div>
    <div class="bni" onclick="swApp('analytics',this)"><span class="ic">📈</span>STATS</div>
    <div class="bni" onclick="swApp('calendar',this)"><span class="ic">📅</span>CAL</div>
  </div>
</div>

<script>
// ===== CONFIG — REPLACE WITH YOUR KEYS =====
var SUPABASE_URL = 'YOUR_SUPABASE_URL';
var SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
var RAZORPAY_LINK = 'YOUR_RAZORPAY_PAYMENT_LINK';
var RAZORPAY_KEY_ID = 'YOUR_RAZORPAY_KEY_ID';
// ===========================================

var supabase = null;
try {
  supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
} catch(e) { console.log('Supabase config needed'); }

var currentUser = null;
var allTrades = [];
var emotionRating = 3;
var screenshotBase64 = null;
var dashDone = false;
var analDone = false;

// ===== FLOATING BOXES =====
(function(){
  var c = document.getElementById('gboxes');
  if(!c) return;
  for(var i=0;i<20;i++){
    var d=document.createElement('div');
    var s=35+Math.random()*110;
    var delay=Math.random()*15;
    var dur=9+Math.random()*18;
    var rot=(Math.random()-0.5)*35;
    d.style.cssText='position:absolute;width:'+s+'px;height:'+s+'px;border:1px solid rgba(245,166,35,'+(0.03+Math.random()*0.07)+');border-radius:8px;left:'+(Math.random()*100)+'%;top:'+(10+Math.random()*100)+'%;animation:gboxFloat '+dur+'s '+delay+'s linear infinite;--r:'+rot+'deg;transform:rotate('+rot+'deg)';
    c.appendChild(d);
  }
})();

// ===== JOURNAL TYPEWRITER =====
var jLines=[
  {id:'jl0',delay:500},{id:'jl1',delay:1000},
  {id:'jl2',delay:1600,type:{el:'jt2',cur:'jc2',text:'Waited for CHoCH confirmation. FVG formed cleanly. Felt confident, no FOMO.'}},
  {id:'jl3',delay:4200},
  {id:'jl4',delay:4800,type:{el:'jt4',cur:'jc4',text:'Patience always pays. Wait for structure before every entry.'}}
];
function typeText(elId,curId,text){
  var el=document.getElementById(elId),cur=document.getElementById(curId);
  if(!el||!cur)return;
  cur.style.display='inline-block';
  var i=0;
  var t=setInterval(function(){
    if(i<text.length){el.textContent+=text[i];i++;}
    else{clearInterval(t);cur.style.display='none';}
  },26);
}
setTimeout(function(){
  jLines.forEach(function(l){
    setTimeout(function(){
      var el=document.getElementById(l.id);
      if(el)el.classList.add('show');
      if(l.type)typeText(l.type.el,l.type.cur,l.type.text);
    },l.delay);
  });
},700);

// ===== AUTH =====
function showPage(id){
  document.querySelectorAll('.page').forEach(function(p){p.classList.remove('active')});
  document.getElementById(id).classList.add('active');
  if(id==='page-dashboard'){
    var n=document.getElementById('appnav');
    if(n)n.style.left=window.innerWidth>768?'230px':'0';
    loadUserData();
  }
}

function switchTab(el,t){
  document.querySelectorAll('.atab').forEach(function(a){a.classList.remove('active')});
  el.classList.add('active');
  document.getElementById('login-form').style.display=t==='login'?'block':'none';
  document.getElementById('signup-form').style.display=t==='signup'?'block':'none';
  showMsg('','');
}

function showMsg(msg,type){
  var el=document.getElementById('auth-msg');
  if(!el)return;
  el.textContent=msg;
  el.className='auth-msg'+(type?' '+type:'');
}

async function handleLogin(){
  if(!supabase){showMsg('Configure Supabase keys first','err');return;}
  var email=document.getElementById('login-email').value;
  var pass=document.getElementById('login-pass').value;
  if(!email||!pass){showMsg('Please fill all fields','err');return;}
  showMsg('Logging in...','');
  var {data,error}=await supabase.auth.signInWithPassword({email,password:pass});
  if(error){showMsg(error.message,'err');return;}
  currentUser=data.user;
  await checkAndRoute();
}

async function handleSignup(){
  if(!supabase){showMsg('Configure Supabase keys first','err');return;}
  var name=document.getElementById('signup-name').value;
  var email=document.getElementById('signup-email').value;
  var pass=document.getElementById('signup-pass').value;
  if(!name||!email||!pass){showMsg('Please fill all fields','err');return;}
  if(pass.length<6){showMsg('Password must be at least 6 characters','err');return;}
  showMsg('Creating account...','');
  var {data,error}=await supabase.auth.signUp({email,password:pass,options:{data:{full_name:name}}});
  if(error){showMsg(error.message,'err');return;}
  // Create profile
  if(data.user){
    await supabase.from('profiles').upsert({id:data.user.id,full_name:name,email:email,is_paid:false});
  }
  showMsg('Account created! Please check your email to verify.','ok');
}

async function checkAndRoute(){
  if(!supabase||!currentUser){showPage('page-dashboard');return;}
  var {data}=await supabase.from('profiles').select('is_paid,full_name').eq('id',currentUser.id).single();
  if(data&&data.is_paid){
    var name=data.full_name||'Trader';
    document.getElementById('greet-msg').textContent='GOOD '+(getTimeOfDay())+', '+name.toUpperCase()+' 👋';
    showPage('page-dashboard');
  } else {
    showPage('page-locked');
  }
}

function getTimeOfDay(){
  var h=new Date().getHours();
  if(h<12)return'MORNING';if(h<17)return'AFTERNOON';return'EVENING';
}

async function handleLogout(){
  if(supabase)await supabase.auth.signOut();
  currentUser=null;allTrades=[];dashDone=false;analDone=false;
  showPage('page-landing');
}

// ===== PAYMENT =====
function handlePayment(){
  if(RAZORPAY_LINK&&RAZORPAY_LINK!=='YOUR_RAZORPAY_PAYMENT_LINK'){
    window.open(RAZORPAY_LINK,'_blank');
  } else {
    alert('Payment gateway not configured yet. Please contact Aditya Barnwal to purchase.');
  }
}

// ===== LOAD DATA =====
async function loadUserData(){
  if(!supabase||!currentUser)return;
  var {data}=await supabase.from('trades').select('*').eq('user_id',currentUser.id).order('date',{ascending:true});
  if(data)allTrades=data;
  updateDashboard();
}

function updateDashboard(){
  var t=allTrades;
  var wins=t.filter(function(x){return x.result==='Win'}).length;
  var total=t.length;
  var wr=total?Math.round(wins/total*100):0;
  document.getElementById('stat-trades').textContent=total;
  document.getElementById('stat-wr').textContent=wr+'%';
  // Simple R calc
  var totalR=0;
  t.forEach(function(tr){
    if(tr.result==='Win')totalR+=2;
    else if(tr.result==='Loss')totalR-=1;
  });
  document.getElementById('stat-r').textContent=(totalR>=0?'+':'')+totalR.toFixed(1)+'R';
  document.getElementById('stat-pf').textContent=wins&&(total-wins)?(wins/(total-wins)).toFixed(2):'—';
  renderRecentTrades(t.slice(-5).reverse());
  renderAllTrades(t.slice().reverse());
  if(!dashDone){initDashCharts(t);dashDone=true;}
  buildCal(t);
}

function renderRecentTrades(trades){
  var el=document.getElementById('recent-trades-body');
  if(!el)return;
  if(!trades.length){el.innerHTML='<tr><td colspan="7" style="text-align:center;color:var(--muted);padding:2rem">No trades yet. Add your first trade!</td></tr>';return;}
  el.innerHTML=trades.map(function(t){
    var d=new Date(t.date);
    var dateStr=d.toLocaleDateString('en-IN',{day:'numeric',month:'short'});
    var rb=t.result==='Win'?'bw':t.result==='Loss'?'bl':'bb';
    return '<tr><td>'+dateStr+'</td><td>'+t.instrument+'</td><td class="'+(t.direction==='BUY'?'db':'ds')+'">'+t.direction+'</td><td>'+t.session+'</td><td>'+(t.strategy_tag||'—')+'</td><td>'+(t.rr_ratio||'—')+'</td><td><span class="'+rb+'">'+t.result.toUpperCase()+'</span></td></tr>';
  }).join('');
}

function renderAllTrades(trades){
  var el=document.getElementById('all-trades-body');
  if(!el)return;
  if(!trades.length){el.innerHTML='<tr><td colspan="11" style="text-align:center;color:var(--muted);padding:2rem">No trades yet.</td></tr>';return;}
  el.innerHTML=trades.map(function(t){
    var d=new Date(t.date);
    var dateStr=d.toLocaleDateString('en-IN',{day:'numeric',month:'short'});
    var rb=t.result==='Win'?'bw':t.result==='Loss'?'bl':'bb';
    var stars='⭐'.repeat(t.emotional_rating||0);
    return '<tr><td>'+dateStr+'</td><td>'+t.instrument+'</td><td class="'+(t.direction==='BUY'?'db':'ds')+'">'+t.direction+'</td><td>'+t.session+'</td><td>'+(t.entry_price||'—')+'</td><td>'+(t.stop_loss||'—')+'</td><td>'+(t.take_profit||'—')+'</td><td>'+(t.rr_ratio||'—')+'</td><td>'+(t.strategy_tag||'—')+'</td><td>'+stars+'</td><td><span class="'+rb+'">'+t.result.toUpperCase()+'</span></td></tr>';
  }).join('');
}

// ===== FORM =====
function calcRR(){
  var e=parseFloat(document.getElementById('f-entry').value);
  var sl=parseFloat(document.getElementById('f-sl').value);
  var tp=parseFloat(document.getElementById('f-tp').value);
  if(e&&sl&&tp&&e!==sl){
    var risk=Math.abs(e-sl);
    var reward=Math.abs(tp-e);
    var rr=(reward/risk).toFixed(1);
    document.getElementById('f-rr').value='1:'+rr;
  }
}

function rateStar(n){
  emotionRating=n;
  document.querySelectorAll('.star').forEach(function(s,i){s.classList.toggle('on',i<n)});
}

function handleFile(file){
  if(!file)return;
  var reader=new FileReader();
  reader.onload=function(e){
    screenshotBase64=e.target.result;
    document.getElementById('ss-img').src=e.target.result;
    document.getElementById('ss-preview').style.display='block';
  };
  reader.readAsDataURL(file);
}

function handleDrop(e){
  e.preventDefault();
  var file=e.dataTransfer.files[0];
  if(file)handleFile(file);
}

async function saveTrade(){
  var btn=document.getElementById('save-btn');
  var msg=document.getElementById('save-msg');
  if(!supabase||!currentUser){
    msg.textContent='Please login first';msg.className='auth-msg err';return;
  }
  btn.textContent='SAVING...';btn.disabled=true;
  var mistake=[];
  document.querySelectorAll('.mtag.on').forEach(function(t){mistake.push(t.textContent)});
  var trade={
    user_id:currentUser.id,
    date:document.getElementById('f-date').value||new Date().toISOString(),
    instrument:document.getElementById('f-inst').value,
    session:document.getElementById('f-sess').value,
    direction:document.getElementById('f-dir').value,
    entry_price:parseFloat(document.getElementById('f-entry').value)||null,
    stop_loss:parseFloat(document.getElementById('f-sl').value)||null,
    take_profit:parseFloat(document.getElementById('f-tp').value)||null,
    rr_ratio:document.getElementById('f-rr').value,
    lot_size:parseFloat(document.getElementById('f-lot').value)||null,
    result:document.getElementById('f-res').value,
    strategy_tag:document.getElementById('f-strat').value,
    pips:parseFloat(document.getElementById('f-pips').value)||null,
    pre_trade_notes:document.getElementById('f-pre').value,
    post_trade_notes:document.getElementById('f-post').value,
    emotional_rating:emotionRating,
    mistake_tag:mistake.join(', '),
    screenshot_url:screenshotBase64||null
  };
  var {error}=await supabase.from('trades').insert(trade);
  btn.textContent='SAVE TRADE ✓';btn.disabled=false;
  if(error){msg.textContent='Error: '+error.message;msg.className='auth-msg err';}
  else{
    msg.textContent='Trade saved successfully!';msg.className='auth-msg ok';
    allTrades.push(trade);
    dashDone=false;updateDashboard();
    setTimeout(function(){swApp('tradelog',null);},1200);
  }
}

// ===== CHARTS =====
var chartInstances={};
function destroyChart(id){if(chartInstances[id]){chartInstances[id].destroy();delete chartInstances[id];}}

function initDashCharts(trades){
  var g='#F5A623',gr='#22c55e',r='#ef4444',m='rgba(136,135,128,0.15)';
  // Equity curve
  var eq=document.getElementById('eq');
  if(eq){
    destroyChart('eq');
    var labels=['Start'];var data=[0];var cum=0;
    trades.forEach(function(t,i){
      labels.push('T'+(i+1));
      if(t.result==='Win')cum+=2;else if(t.result==='Loss')cum-=1;
      data.push(parseFloat(cum.toFixed(2)));
    });
    if(data.length===1){labels.push('T1');data.push(0);}
    chartInstances['eq']=new Chart(eq,{type:'line',data:{labels:labels,datasets:[{data:data,borderColor:g,backgroundColor:'rgba(245,166,35,0.06)',fill:true,tension:.4,pointRadius:3,pointBackgroundColor:g,borderWidth:2}]},options:{responsive:true,plugins:{legend:{display:false}},scales:{x:{grid:{color:m},ticks:{color:'#666',font:{size:9}}},y:{grid:{color:m},ticks:{color:'#666',font:{size:9}}}}}});
  }
  var wl=document.getElementById('wl');
  if(wl){
    destroyChart('wl');
    var wins=trades.filter(function(t){return t.result==='Win'}).length;
    var losses=trades.filter(function(t){return t.result==='Loss'}).length;
    var be=trades.filter(function(t){return t.result==='Breakeven'}).length;
    chartInstances['wl']=new Chart(wl,{type:'doughnut',data:{labels:['Win','Loss','BE'],datasets:[{data:[wins||1,losses,be],backgroundColor:[gr,r,g],borderWidth:0,borderRadius:3}]},options:{responsive:true,cutout:'68%',plugins:{legend:{position:'bottom',labels:{color:'#888',font:{size:10},padding:10}}}}});
  }
}

function initAnalyticsCharts(){
  if(analDone)return;analDone=true;
  var g='#F5A623',gr='#22c55e',r='#ef4444',m='rgba(136,135,128,0.15)';
  var baseOpts={responsive:true,plugins:{legend:{display:false}},scales:{x:{grid:{color:m},ticks:{color:'#666',font:{size:9}}},y:{grid:{color:m},ticks:{color:'#666',font:{size:9}}}}};
  var t=allTrades;

  // By instrument
  var insts=['US30','XAUUSD','EURUSD','GBPUSD','NAS100'];
  var instR=insts.map(function(ins){
    var it=t.filter(function(x){return x.instrument===ins});
    var r=0;it.forEach(function(x){if(x.result==='Win')r+=2;else if(x.result==='Loss')r-=1;});return r;
  });
  var ic=document.getElementById('ic');
  if(ic){destroyChart('ic');chartInstances['ic']=new Chart(ic,{type:'bar',data:{labels:insts,datasets:[{data:instR,backgroundColor:instR.map(function(v){return v>=0?g:r}),borderRadius:5}]},options:baseOpts});}

  // By session
  var sess=['London','New York','Asian','Overlap'];
  var sessR=sess.map(function(s){
    var st=t.filter(function(x){return x.session===s});
    var rv=0;st.forEach(function(x){if(x.result==='Win')rv+=2;else if(x.result==='Loss')rv-=1;});return rv;
  });
  var sc=document.getElementById('scc');
  if(sc){destroyChart('scc');chartInstances['scc']=new Chart(sc,{type:'bar',data:{labels:sess,datasets:[{data:sessR,backgroundColor:gr,borderRadius:5}]},options:baseOpts});}

  // Mistakes
  var mkeys=['Perfect Execution','FOMO','Moved SL','Revenge Trade','Overleverage'];
  var mvals=mkeys.map(function(k){return t.filter(function(x){return x.mistake_tag&&x.mistake_tag.includes(k)}).length});
  var mc=document.getElementById('mcc');
  if(mc){destroyChart('mcc');chartInstances['mcc']=new Chart(mc,{type:'bar',data:{labels:mkeys,datasets:[{data:mvals,backgroundColor:[gr,r,r,r,r],borderRadius:5}]},options:baseOpts});}

  // Strategy win rate
  var strats=['FVG','Order Block','CHoCH','Liquidity Sweep','BOS'];
  var stratWR=strats.map(function(s){
    var st=t.filter(function(x){return x.strategy_tag===s});
    if(!st.length)return 0;
    return Math.round(st.filter(function(x){return x.result==='Win'}).length/st.length*100);
  });
  var stc=document.getElementById('stcc');
  if(stc){destroyChart('stcc');chartInstances['stcc']=new Chart(stc,{type:'bar',data:{labels:strats,datasets:[{data:stratWR,backgroundColor:g,borderRadius:5}]},options:baseOpts});}

  // Update analytics stats
  var bestSessIdx=sessR.indexOf(Math.max.apply(null,sessR));
  document.getElementById('a-sess').textContent=sessR[bestSessIdx]>0?sess[bestSessIdx]:'—';
  var bestInstIdx=instR.indexOf(Math.max.apply(null,instR));
  document.getElementById('a-inst').textContent=instR[bestInstIdx]>0?insts[bestInstIdx]:'—';
  // Streak
  var streak=0,maxStreak=0,cur=0;
  t.forEach(function(tr){if(tr.result==='Win'){cur++;streak=Math.max(streak,cur);}else cur=0;});
  document.getElementById('a-streak').textContent=streak;
  // Avg emotion
  var emos=t.filter(function(x){return x.emotional_rating}).map(function(x){return x.emotional_rating});
  var avgEmo=emos.length?( emos.reduce(function(a,b){return a+b},0)/emos.length).toFixed(1):0;
  document.getElementById('a-emo').textContent=avgEmo+'★';
}

function buildCal(trades){
  var g=document.getElementById('calg');
  if(!g)return;
  g.innerHTML='';
  var now=new Date();
  var yr=now.getFullYear(),mo=now.getMonth();
  var firstDay=new Date(yr,mo,1).getDay();
  var daysInMonth=new Date(yr,mo+1,0).getDate();
  // Adjust to Mon start
  var offset=(firstDay+6)%7;
  for(var i=0;i<offset;i++){var b=document.createElement('div');b.style.height='52px';g.appendChild(b);}
  for(var d=1;d<=daysInMonth;d++){
    var dayTrades=trades.filter(function(t){
      var td=new Date(t.date);
      return td.getFullYear()===yr&&td.getMonth()===mo&&td.getDate()===d;
    });
    var div=document.createElement('div');div.className='cday';
    var wins=dayTrades.filter(function(t){return t.result==='Win'}).length;
    var losses=dayTrades.filter(function(t){return t.result==='Loss'}).length;
    if(dayTrades.length){
      if(wins>=losses)div.classList.add('win');else div.classList.add('loss');
      var r=0;dayTrades.forEach(function(t){if(t.result==='Win')r+=2;else if(t.result==='Loss')r-=1;});
      div.innerHTML='<span>'+d+'</span><span class="dp">'+(r>=0?'+':'')+r.toFixed(1)+'R</span>';
    } else {
      div.innerHTML='<span>'+d+'</span>';
    }
    g.appendChild(div);
  }
}

// ===== NAVIGATION =====
function swApp(v,el){
  document.querySelectorAll('[id^="v-"]').forEach(function(x){x.style.display='none'});
  var view=document.getElementById('v-'+v);
  if(view)view.style.display='block';
  document.querySelectorAll('.ni').forEach(function(n){n.classList.remove('active')});
  document.querySelectorAll('.bni').forEach(function(n){n.classList.remove('active')});
  if(el)el.classList.add('active');
  if(v==='analytics'){setTimeout(initAnalyticsCharts,150);}
  if(v==='calendar'){buildCal(allTrades);}
  // Set date default
  if(v==='addtrade'){
    var now=new Date();
    var local=new Date(now.getTime()-now.getTimezoneOffset()*60000).toISOString().slice(0,16);
    document.getElementById('f-date').value=local;
  }
}

// ===== INIT AUTH STATE =====
window.addEventListener('DOMContentLoaded',async function(){
  if(!supabase)return;
  var {data}=await supabase.auth.getSession();
  if(data.session){
    currentUser=data.session.user;
    await checkAndRoute();
  }
});

window.addEventListener('resize',function(){
  var n=document.getElementById('appnav');
  if(n&&document.getElementById('page-dashboard').classList.contains('active'))
    n.style.left=window.innerWidth>768?'230px':'0';
});
</script>
</body>
</html>
